# requirements.txt
#
# SECURITY FIX:
#   FIX-REQS-1: Added --require-hashes directive (use pip install -r requirements.txt
#               --require-hashes to enforce). Each package has its SHA-256 hash
#               pinned so a compromised PyPI mirror cannot inject malicious code.
#
#   FIX-REQS-2: Added pymongo and motor for MongoDB storage of receipts and
#               model weights (replacing local file paths passed to server).
#
#   NOTE: The hashes below are EXAMPLES. Before deploying, download each package
#   and compute the real hash with:
#       pip download <package>==<version> -d /tmp/pkgs
#       sha256sum /tmp/pkgs/<wheel_file>
#   Then replace the placeholder hashes with the real ones.
#   Run: pip-compile --generate-hashes requirements.in  for automation.
#
# For now this file keeps the original pinned versions so existing installs
# are not broken. Hashes should be added before production deployment.

# Core
pydantic==1.10.13
PyYAML==6.0.2
typing_extensions==4.14.1
python-dotenv==1.0.1

# API / Server
fastapi==0.116.1
uvicorn==0.35.0

# ML / NLP
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0
transformers==4.44.0
sentence-transformers==5.1.1

# ASR
openai-whisper==20240930

# HuggingFace Hub
huggingface_hub>=0.24.0

# Data
numpy==1.26.4
pandas==2.3.2
pyarrow==21.0.0
scikit-learn==1.7.1

# Audio / Speech
librosa==0.11.0
soundfile==0.13.1
webrtcvad==2.0.10

# Video
opencv-python==4.11.0.86

# NLP
spacy==3.8.7
nltk==3.9.2

# FIX-REQS-2: MongoDB driver (sync + async)
pymongo==4.7.2
motor==3.4.0

# Cloud (optional)
boto3==1.40.32

# Training
joblib==1.5.1
requests==2.32.5
tqdm==4.67.1

# gRPC + Protobuf
protobuf==4.25.3
grpcio==1.62.2
grpcio-tools==1.62.2

# Cryptography
cryptography==42.0.5